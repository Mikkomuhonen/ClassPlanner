## 1. Select all groups control

- [x] 1.1 Add a "Valitse kaikki ryhmät →" button above the per-group list in `renderPopupParticipants()` that adds every member of every group to `popupParticipants`, skipping duplicates — verified by opening the popup and clicking the control with an empty and non-empty selection
- [x] 1.2 Add CSS for the new button/row — verified visually that it is clearly distinguished from the per-group buttons

## 2. Documentation

- [x] 2.1 Document the select-all-groups control and its interaction with individual removal in the `week-planner` spec (this change)
