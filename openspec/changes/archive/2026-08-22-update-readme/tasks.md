## 1. README update

- [x] 1.1 Replace the contents of `README.md` with a complete application description including: title and purpose paragraph, feature list (week planner grid, per-day rhythms and breaks, participant registry, lesson popup editor, copy/undo, lunch marker, folder persistence), browser requirement, usage instructions (npm start → http://localhost:3000 → select folder), and the existing setup section — verify the file is saved and contains all sections
